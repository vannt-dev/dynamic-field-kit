---
---

No release needed. vitest, vite, jsdom and the analog plugins are all
devDependencies; `files` publishes `dist` alone, and the built `dist` for all
four packages is byte-identical to the previous commit.
