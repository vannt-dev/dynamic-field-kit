---
---

No release needed. This branch only widens what eslint looks at and fixes the
errors that were hiding outside its old scope. No `src` file changed, so the
built `dist` for all four packages is untouched.
