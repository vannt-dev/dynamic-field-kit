# @dynamic-field-kit/core

## 1.3.0

### Minor Changes

- 132f74b: Add validation & dynamic conditions engine, standalone validators, and unified
  layout helpers to the core package.

  - New `validation` API (field-level validation, dynamic conditions).
  - New `validators` API (reusable, framework-agnostic validators).
  - New `layout` helpers and shared layout types.
  - Extended `computeValues`/`applyComputedValues` and `FieldRegistry`.
